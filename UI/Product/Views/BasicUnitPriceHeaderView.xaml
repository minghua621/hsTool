<UserControl x:Class="UI.Product.Views.BasicUnitPriceHeaderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:product="clr-namespace:UI.Product.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <Style TargetType="{x:Type Border}" BasedOn="{StaticResource BlueTopBrushBorder}"/>
    </UserControl.Resources>
    <Grid>
        <TabControl x:Name="tabControl" TabStripPlacement="Top" ItemContainerStyle="{StaticResource TabItemStyle_Blue}"/>
    </Grid>
</UserControl>
